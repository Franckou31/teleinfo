'use strict';

describe('myApp.dayStat module', function() {

  beforeEach(module('myApp.dayStat'));

  describe('dayStat controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dayStatCtrl = $controller('dayStatCtrl');
      expect(dayStatCtrl).toBeDefined();
    }));

  });
});

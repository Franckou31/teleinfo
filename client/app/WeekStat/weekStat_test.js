'use strict';

describe('myApp.weekStat module', function() {

  beforeEach(module('myApp.weekStat'));

  describe('weekStatCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var weekStatCtrl = $controller('weekStatCtrl');
      expect(weekStatCtrl).toBeDefined();
    }));

  });
});
